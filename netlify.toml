[build]
  # Other build settings might go here

[context.production]
  # Set the Node version for the production build
  NODE_VERSION = "24.12.0"

[context.branch-deploy]
  # Optionally set the Node version for branch builds
  NODE_VERSION = "24.12.0"

[context.deploy-preview]
  # Optionally set the Node version for deploy previews
  NODE_VERSION = "24.12.0"
